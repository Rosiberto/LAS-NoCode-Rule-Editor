name: Sync Main to v2.0.0

on:
  push:
    branches:
      - main

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v2
<<<<<<< HEAD
=======
        with:
          fetch-depth: 0
>>>>>>> origin/main

      - name: Set up Git
        run: |
          git config user.name "Rosiberto"
          git config user.email "rosibertosantos@gmail.com"

      - name: Checkout v2.0.0 branch
        run: git checkout v2.0.0

      - name: Merge main into v2.0.0
        run: git merge main --no-ff --commit

      - name: Push changes to v2.0.0
        run: git push origin v2.0.0
